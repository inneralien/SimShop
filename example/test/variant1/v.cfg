[DEFAULT]
#DEFINES = VERBOSE_RTL
#PLUSARGS = DUMPON

PROJ_ROOT = ../../

RTL_FILES = rtl/and_nand.v
            rtl/or_nor.v

DUMPVARS = (0,tb) (0,test_util)

TEST_FILES =    test/variant1/tb.v 
                test/variant1/test_util.v

TEST_INC_DIRS = test/variant1/

[basic]
TASKS = tb.run_sim
